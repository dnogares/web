version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./capas:/app/capas
      - ./outputs:/app/outputs
      - ./catastro_cache.sqlite:/app/catastro_cache.sqlite
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
